swagger: '2.0'
host: https://api-ip.fssprus.ru/api/v1.0/
schemes:
  - https
security:
  - APIKeyHeader: [] 
paths:
  '/search/physical?region={region}':
    get:
      
      
 
      parameters:
      

      
      - name: firstname
        in: path
      responses:
        200:
          description: OK
          schema:
            type: object
            properties:
              now:
                type: object
                properties:
                  rfc2822:
                    type: string
/*      responses:
        '200':
          description: successful operation
          headers:
              Access-Control-Allow-Origin: "'https://api-ip.fssprus.ru'"
              Access-Control-Allow-Methods: GET, POST, DELETE, PUT, PATCH, OPTIONS
              Access-Control-Allow-Headers: Content-Type, api_key, Authorization

 */     
  <SwaggerUI url="/swagger.yaml"  plugins= [DisableTryItOutPlugin]
 
 />
securityDefinitions:

       # /path?api_key=abcdef12345
       APIKeyQueryParam:
         type: apiKey
         in: query
         name: token 
  
    

